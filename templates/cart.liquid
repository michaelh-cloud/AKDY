{% comment %}
  Full cart page is built in sections/main-cart.liquid (with a schema, so
  every label on the page is editable from the theme customizer). This
  template just renders that section — kept as a plain .liquid template
  rather than converting to templates/cart.json + deleting this file,
  since theme file deletion is blocked as a safety measure on this
  connection. Functionally identical to a JSON template pointing at the
  same section.
{% endcomment %}
{% section 'main-cart' %}
